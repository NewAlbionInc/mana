/* ----Loic's desperate attempt to create cool animations but failing miserably------
function scrollAppear(){
    var dutexte = document.querySelector(.page-top);
    var pos = text.getBoundingClientRect().top;
    var screenpos = window.innerHeight;/1.2

    if (pos < screenpos) {
        text.classList.add('page-top-appear');
    }
}

window.addEventListener('scroll', scrollAppear);
*/